# Contributing

感谢参与 YitDesktopFold。

## 开始之前

- 功能应服务于轻量桌面整理，避免加入账号、云服务、内容中心或持续后台索引。
- 不得通过移动/复制真实桌面文件实现分类。
- 修改 Hidden 属性、Shell 虚拟图标注册表或 Explorer 层级时，必须先保存原状态、保证可逆，并限制到应用明确拥有的项目。
- 保持未归类的 Explorer 图标、原生框选、桌面菜单和 `Win+D` 行为。

## 提交要求

1. 从独立分支提交聚焦改动。
2. 运行原生测试及相关 Web/Sites 测试。
3. 涉及状态模型时验证旧版本迁移、损坏恢复和失败回滚。
4. 涉及 UI 时同时验证 100%/高 DPI、黑白底色、纯图标模式与减少动态效果设置。
5. PR 描述中说明对文件系统、注册表、Shell、内存和兼容性的影响。

不要提交 `bin/`、`obj/`、`dist/`、`artifacts/`、安装包、测试截图、证书或本机配置。
