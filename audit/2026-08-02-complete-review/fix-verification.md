# 审查问题修复验证

验证日期：2026-08-02

原审查中列出的 2 个 P1、6 个 P2、2 个 P3/性能风险均已处理。

- `Win+D`：已从一次性 Z 轴补位升级为 250ms Show Desktop 状态探针，兼容 Windows 11 24H2 的 `Progman` 桌面图标宿主和旧版 `WorkerW` 宿主；三个整理块在持续显示桌面 4.7 秒后仍可见，见 `09-worker-layer-show-desktop.png`。
- 无闪烁切换：监听 Explorer 桌面容器的 `EVENT_OBJECT_REORDER`，并用前台切换后的 2–10ms 快速探测处理竞争条件。高速 Z 轴探针在切换后的 120ms 内没有观察到整理块落到桌面宿主之后；修复前可复现约 16ms 的隐藏间隙。
- 普通窗口层级：全屏记事本完全覆盖整理块；见 `08-fixed-under-notepad.png`。
- 新桌面层退出验证：退出显示桌面后探针锚点撤回底层，全屏记事本继续完整覆盖整理块；见 `10-worker-layer-under-app.png`。
- 键盘路径：`Ctrl+Alt+D` 进入最近使用的整理块，UI Automation 焦点落到首个快捷方式；`Escape` 恢复先前前台并重新启用 `WS_EX_NOACTIVATE`。
- 尺寸吸附：宽高独立验证；支持上、左、右邻居和 10 DIP 小幅越界；新拖动会取消上一次释放动画。
- 短屏设置：窗口高度按当前工作区收缩，主体可滚动，底部操作固定可见。
- 全局窗口：设置和重命名弹窗运行时均带 `WS_EX_APPWINDOW`，可从任务栏找回。
- 高透明度：92% 以上按整理块所在桌面区域亮度选择中性深/浅前景。
- 稳定性：配置/注册表权限异常安全降级；共享 Shell 图标缓存最多保留 192 个成功结果。

Release 严格构建为 0 警告、0 错误，全部原生自动化检查通过。验证过程未改写用户 `state.json`。
